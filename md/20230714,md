# 7월 14일 학습내용
## 안드로이드 스튜디오
### TextWatcher
- 유저가 입력할때 보여주는 기능
- DB에서 가져올 때 select 문으로 가져온다.


### Json 샘플 url 1
- 학습용 fake data
- https://jsonplaceholder.typicode.com/
###
- 안드로이드 네트워크 라이브러리
  - Volley
  - 

## 에뮬레이터를 이용해서 네트워크 통신
#### 설정
1. res/xml 폴더안에 network_security_config.xml 생성
```xml
<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">*.typicode.com</domain>
    </domain-config>
</network-security-config>
```
2. AndroidManifest.xml 수정
3. volley 라이브러리 싱크
   - Gradle Scripts의 build gradle에 입력
     - implementation 'com.android.volley:volley:1.2.1'
   - FIFO(통신) : 시간과 우선 순위와 관련된 데이터를 정리하고 이용하는 방식
