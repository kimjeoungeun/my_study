# 변수 : 재사용 가능
# 변수 이름 규칙
# 영어, 숫자, _(언더바)만 사용(한글도 가능)
# 공백 불가
# 키워드(파이썬 안에서 사용되는 이름_ex if, true, false) 사용 불가
# 제일 앞에는 숫자가 오면 안된다.
# 보통 영어가 앞에 오고 _는 특별한 경우에 앞에 위치
# name = "김정은" # 변수를 만들땐 무조건 값을 할당해야함
# first_name = "정은"
# last_name = "김"
# name = last_name + first_name # 변수안에 있는 값 수정 가능(값이 새롭게 할당)
# print(name)

a = 6
b = 2
print(a + b)
print(a - b)
print(a * b)
print(a / b)
print(a ** b) # 제곱
print(a // b) # 몫
print(a % b) # 나머지

# 상수 : 변하지않는 값(재사용, 수정 불가)